---
description: Automatically push code changes to git after edits
alwaysApply: true
---

# Git Push After Changes

After making any code changes to this project:

1. Stage all changes: `git add .`
2. Commit with a descriptive message summarizing the changes
3. Push to the remote: `git push origin main`

Always include this step at the end of your workflow when you have modified files.
